from setuptools import setup

setup(
  name='dask-template',
  packages=['daskfunc'],
  version='0.1',
  description='A demo library to work with dask',
  url='https://github.com/necosta/dask-template',
  keywords=['dask', 'example'],
  classifiers=[],
  install_requires=[]
)
